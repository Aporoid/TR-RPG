using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// used to give BattleSystem a break, hopefully.
/// </summary>
public class TemporaSelector : MonoBehaviour
{
	[SerializeField]
	private GameObject temporaSpellPrefab;

	[SerializeField]
	private Transform temporaListContentArea;

	// Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
